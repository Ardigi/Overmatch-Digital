# Control Service Test Configuration
NODE_ENV=test

# Service Configuration
PORT=3005

# Test Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=soc_user
DB_PASSWORD=soc_pass
DB_NAME=soc_controls_test

# Redis (for Bull queues)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=soc_redis_pass

# Kafka (for events) - disabled for tests
KAFKA_BROKERS=localhost:9092

# JWT Secret (shared with auth service)
JWT_SECRET=test-jwt-secret

# Control Testing Configuration
DEFAULT_TEST_FREQUENCY_DAYS=90
MAX_TEST_RETRIES=3
TEST_TIMEOUT_MS=300000